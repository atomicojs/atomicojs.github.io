{
  "name": "www",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "devserver dev site --port 8080  --jsxImportSource atomico",
    "build": "devserver build site/**/*.html docs --minify --jsxImportSource atomico"
  },
  "author": "",
  "license": "ISC",
  "imports": {
    "atomico": "http://unpkg.com/atomico"
  },
  "dependencies": {
    "atomico": "^1.12.1"
  },
  "devDependencies": {
    "@devserver/build": "^0.12.2",
    "@devserver/cli": "^0.12.2"
  }
}
